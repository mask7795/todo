# Changelog

## v0.2.0 — 2025-12-13

- Setup: optional `nvm install` via `SETUP_NVM_INSTALL=1` or `--install-nvm` in `scripts/setup-frontend.sh`.
- Frontend: added `setup:frontend:nvm-install` npm alias.
- Docs: README updated with setup flag usage and commands.

Notes:
- This release improves developer ergonomics and CI consistency for Node version alignment.